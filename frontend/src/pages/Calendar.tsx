/**
 * Calendar — Etapa 1
 *
 * Vista mensual con:
 * - Días con evento de nutrición (naranja)
 * - Días con riego simple (azul)
 * - Días de observación/foto (verde)
 * - Al hacer click en un día → detalle de tareas del día
 */
export default function Calendar() {
  return <div>Calendar — Etapa 1</div>
}
