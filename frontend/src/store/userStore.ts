/**
 * userStore — estado del usuario (Zustand)
 *
 * State:
 * - plan: AccessTier ('free' | 'pro')
 * - name: string
 * - potVolumeLiters?: number  (preferencia global de volumen de maceta)
 *
 * Implementar en Etapa 1
 */
export {}
