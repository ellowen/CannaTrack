/**
 * plantStore — estado global de plantas (Zustand)
 * Persistencia: localStorage (Etapa 1) → Supabase (Etapa 2)
 *
 * State: plants: Plant[]
 * Actions: addPlant, updatePlant, removePlant
 *
 * Implementar en Etapa 1
 */
export {}
