/**
 * Rutas de la app — se implementan en Etapa 1
 *
 * Rutas planificadas:
 * /              → Home (plantas activas + tareas del día)
 * /plants/new    → NewPlant (formulario de alta)
 * /plants/:id    → PlantDetail (detalle, calendario, historial)
 * /calendar      → Calendar (vista mensual)
 * /settings      → Settings (plan, preferencias)
 */
export {}
