/**
 * usePlants — CRUD de plantas desde el store
 *
 * Returns:
 * - plants: Plant[]               todas las plantas activas
 * - addPlant(plant)               agregar nueva planta
 * - updatePlant(id, changes)      actualizar campos de una planta
 * - discardPlant(id)              marcar como descartada (macho)
 * - harvestPlant(id)              marcar como cosechada
 * - getPlantById(id): Plant       buscar por id
 *
 * Implementar en Etapa 1
 */
export {}
