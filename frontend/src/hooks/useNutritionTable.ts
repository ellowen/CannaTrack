/**
 * useNutritionTable — acceso a tablas nutricionales
 *
 * Returns:
 * - availableTables: NutritionTable[]    tablas accesibles según plan
 * - getTableById(id): NutritionTable     buscar tabla por id
 * - canAccess(table): boolean            verificar acceso según plan
 *
 * Implementar en Etapa 1
 */
export {}
